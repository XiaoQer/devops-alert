# Incident Intelligence Agent 工作指南

本仓库采用规格先行、任务拆分、测试驱动和分阶段验收的开发方式。项目事实以仓库为准，不以聊天记录为准。

## 每次任务开始前

1. 阅读 `docs/product.md`、`docs/architecture.md` 和 `docs/current-state.md`。
2. 阅读 `specs/active/` 中对应规格；行为变更没有规格时，先从 `specs/template.md` 创建。
3. 检查工作区状态并保留用户已有修改。
4. 文档描述意图，代码、测试和可执行验证证明当前行为。

## 核心边界

- 本项目只负责多源信号接入、告警归集、事故关联、事故运营、自动取证和 AI 分析。
- 本项目不得包含故障场景、注入器、实验控制器、Chaos 资源写权限、实验恢复或评测标准答案。
- 本项目不得读取故障注入平台的数据库、源码模块或内部状态。
- `scenario_id`、`scenario_version`、`experiment_id`、注入动作和标准答案不得进入信号、告警、事故、证据快照、AI 输入或报告。
- AI 不直接访问或生成监控平台查询，只分析系统执行版本化预定义只读查询后形成的标准化证据。
- SignalEvent、Alert、Incident 和 DiagnosisRun 使用独立模型；告警、事故运营和诊断任务使用独立状态机。
- Secret 不得进入源码、镜像、日志、API 响应、测试数据或事故快照。
- 计划能力必须明确标注，不能写成已实现能力。

## 变更流程

1. 在活跃规格中记录背景、范围、非目标和验收条件。
2. 把规格拆成可独立验证的小任务。
3. 先编写失败测试，再实现最小完整改动。
4. 运行仓库统一验证脚本。
5. 能力变化时更新 `docs/current-state.md`；长期决策写入 `docs/decisions/`。
6. 分阶段验收通过后，将规格移入 `specs/completed/`。

## 完成定义

- 每条验收条件都有测试或人工验证证据。
- 自动化测试、静态检查和构建全部通过。
- 多源输入幂等、安全、有界且可审计。
- 关联引擎能够解释关联或不关联的理由，并在失败时安全退化。
- AI 或数据源不可用不会阻断人工事故处置。
- 文档反映最终状态并明确记录已知缺口。
