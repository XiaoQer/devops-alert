# Incident 监控数据自动取证

## 状态

正式规格已确认，实现与自动化验证完成；Prometheus 已使用真实 MySQL 故障指标完成基线与故障窗口联调，等待 Elasticsearch、SkyWalking 和用户页面验收。

## 背景与目标

平台已经能够从真实 Alert 创建正式 Incident，但当前 Incident 只展示触发告警和人工处置记录，不能自动保存故障发生时的监控证据。本规格建立异步自动取证基线：使用 Prometheus、ELK 和 SkyWalking 的版本化只读查询，把指标、日志、链路和确定性跨源关联转换成标准证据，并在 Incident 中集中展示。

完整设计见 `docs/superpowers/specs/2026-09-02-monitoring-evidence-collection-design.md`，实施计划见 `docs/superpowers/plans/2026-09-02-monitoring-evidence-collection.md`。

## 范围

- 测试环境一套 Prometheus、Elasticsearch 和 SkyWalking 数据源；
- Incident 创建后自动取证与人工重新取证；
- 基线窗口和故障窗口对照；
- 通用、HTTP、JVM、MySQL 版本化取证包；
- 持久化 EvidenceRun、EvidenceItem 和异步任务；
- 三类只读适配器与有界标准化结果；
- 确定性跨源关联；
- Incident 取证 API、健康摘要和第三版页面结构。
- 监控数据源配置页面，以及 Prometheus、Elasticsearch、SkyWalking 的真实只读连接检测；
- 持久化最近一次检测状态、耗时、兼容版本、错误码和检测时间。

## 非目标

- AI、RAG、根因结论、自动修复或处置建议；
- AI 或用户生成监控查询；
- 多集群、多套同类型数据源或跨环境取证；
- Kubernetes、故障注入平台、发布系统和配置中心；
- 保存无限原始监控数据；
- 用户自定义取证包。

## 设计

- Incident 创建事务写入一条自动 EvidenceRun 和持久化取证任务；
- Worker 根据 Incident 与 Alert 确定 environment、service、Alertname 和锚点；
- `common-service` 必选，HTTP/JVM/MySQL 包按 Alertname 和结构化标签追加；
- 基线窗口为锚点前 30 至前 10 分钟，故障窗口为锚点前 10 分钟至运行开始，最多查询两小时；
- 三个适配器独立失败，成功结果继续保存；
- 历史运行不可覆盖，人工重新取证创建新运行；
- Incident 同时最多一个运行中任务；
- 页面以关键发现为主线，证据向下展开，右侧展示来源状态、时间窗口和规则包。

## 安全与恢复

- Secret 只从运行环境读取，不进入数据库、源码、日志、API、测试数据或证据；
- 适配器只执行版本化预定义只读查询；
- 日志字段白名单、敏感模式脱敏，指标、日志和 Trace 全部有硬上限；
- Worker 使用租约、幂等和有界重试；
- 监控系统不可用不阻塞告警接收、Incident 创建和人工处置；
- 不读取或保存故障注入身份与内部状态。
- 连接检测只访问固定健康或版本接口，不接受用户提交查询文本；
- 连接错误只返回稳定错误码，不向 API、页面或日志泄露响应正文、凭据或连接细节。

## 验收条件

- 新建 Incident 同事务产生且只产生一个自动取证任务；
- 人工重新取证幂等，历史运行不被覆盖；
- 同一 Incident 不会并发运行两个取证任务；
- service 缺失时不猜测，不依赖 service 的证据仍可执行；
- 基线和故障窗口计算准确且不超过两小时；
- Prometheus、ELK、SkyWalking 只执行允许的模板并输出标准 EvidenceItem；
- 单数据源失败形成部分成功，不阻断其他证据或 Incident；
- 日志、指标和 Trace 的条数、字段、长度和敏感信息受控；
- 跨源关联只描述可证明事实，不输出根因或置信度；
- 数据源配置按环境隔离，每环境每类型最多一个启用来源；
- Incident 页面展示历史运行、关键发现、证据展开、来源状态和查询依据；
- Prometheus 标签选择器按数据源映射生成：`service` 未配置时默认使用同名标签，`environment` 未配置时不强制追加环境标签；
- Prometheus 标签映射值必须是合法标签名，不能借字段映射注入查询语句；
- MySQL 取证包读取 exporter 实际暴露的当前行锁等待、累计行锁等待增量和累计行锁等待时间增量，分别呈现故障窗口和基线窗口；
- 操作员可在独立页面创建、编辑、启停并检测三类监控数据源；
- Prometheus、Elasticsearch 和 SkyWalking 的连接检测执行真实只读请求，并保存最近检测结果；
- 缺少凭据、协议响应不兼容、超时和连接失败均显示可理解的中文状态，且不影响 Incident 人工处置；
- 统一后端验证、前端测试、生产构建和真实浏览器主流程全部通过。

## 验证证据

- 领域、仓储、数据源 API、取证计划、三个适配器和跨源关联均经历失败测试再实现；
- 格式与严格类型检查通过；
- 0019 迁移升级、降级、ORM 一致性、唯一边界和仓储租约定向测试通过；
- Incident 创建、唯一自动运行、部分失败、租约回收、人工幂等重取和历史读取集成测试通过；
- 三类真实连接检测的固定请求、协议校验、安全错误和检测结果持久化测试通过；
- 统一后端验证 462 项测试全部通过，覆盖率 90.16%；前端 83 项测试和生产构建通过；
- 本机 Prometheus 3.4.2 真实连接与 Incident 重新取证通过；数据源未配置环境标签映射时安全省略环境条件，服务标签默认使用 `service`。
- `INC-20260902-002` 使用 `common-service:v2` 与 `mysql:v2` 重新取证，服务可用性、当前行锁等待、行锁等待增量和行锁等待耗时增量均取得基线与故障期数据；Elasticsearch、SkyWalking 和真实浏览器页面验收尚未完成，本规格保持 active。
