# Incident 运营与飞书协同验证记录

## 验证结论

截至 2026-09-01，Incident 自动生成、人工处置、飞书通知和群聊记录回流已完成本地契约与跨模块验证；真实 Alert 驱动的浏览器确认与解决流程也已完成。真实飞书测试应用/测试群沙箱联调仍待执行，因此活跃规格暂不归档。

## 自动化验证

- 后端统一验证：Ruff、格式检查和 mypy 全部通过；360 项测试通过；总覆盖率 90.16%。
- 前端统一验证：22 个测试文件、71 项测试全部通过。
- 前端生产构建：Vite 构建成功，并成功生成 Sites 构建产物。
- 跨模块链路：通过 Alertmanager 接口接收一条真实结构的 firing 告警，由已发布规则异步生成正式 Incident；通知 Worker 发送并绑定飞书根卡片；签名合法的线程 `@机器人` 文字写入 Incident 活动；最终通过带版本和幂等键的 API 解决 Incident。
- 安全检查：健康响应不包含任务 ID、payload、异常堆栈、Chat ID、API Token 或飞书 Secret。

## 浏览器验证

- 地址：`http://127.0.0.1:5173/`
- Incident 主导航、真实空列表、筛选区和无演示数据空态通过。
- 飞书事故群配置页、凭据完整性提示、无 Secret 输入框和保存禁用状态通过。
- 后端迁移至 `0018_restore_system_sources` 后，缺失的系统接入源被幂等恢复，现有用户来源保持不变，兼容 Alertmanager 入口恢复接收。
- 通过兼容 Alertmanager 入口接收两条真实结构 firing 告警，创建并发布受控规则，由后台异步生成 `INC-20260901-001`。
- 三栏详情正确展示规则依据、两条关联告警、严重级别升级和飞书未配置状态；浏览器依次完成确认与填写说明后解决。
- 操作后页面重新读取完整详情，确认与解决活动立即进入时间线；已解决 Incident 从默认列表移除，解决表单关闭。

## 外部验证缺口

- 尚未使用真实飞书测试应用和测试群验证根卡片发送、卡片更新、线程回复、重复回调、伪造签名拒绝和临时失败重试。
- 外部沙箱验证前只从本地环境变量读取凭据；验证记录不得包含任何凭据值。
- 完成飞书沙箱验证后，再将规格移入 `specs/completed/`。
