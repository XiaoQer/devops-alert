# 手动 Incident 规则配置验收记录

## 验收结论

手动 Incident 规则管理闭环已完成。本阶段只管理和验证规则，发布不会创建候选 Incident 或正式 Incident。

## 自动化证据

- 后端统一验证：`./scripts/verify-backend.sh`
  - Ruff 格式与检查通过；
  - MyPy 通过；
  - 263 项测试全部通过；
  - 总覆盖率 90.42%。
- 前端规则专项测试：11 项通过；
- 前端完整测试：49 项通过；
- Vite 生产构建通过；
- Sites 打包契约 4 项通过。

## 浏览器证据

- 真实空列表不生成演示规则；
- 四步向导可完成名称、环境、服务、聚合、窗口和结构化条件配置；
- 条件类型可直接选择，其他行已使用的类型不可重复选择；
- 草稿保存到 MySQL 后显示后端生成的中文说明；
- 最近 6 小时真实历史试运行完成，返回扫描数量和命中数量；
- 未试运行时发布按钮禁用，成功且未截断后开放；
- 页面明确说明发布规则不会创建 Incident；
- 浏览器控制台没有 error 或 warning；
- 走查产生的临时草稿、试运行和操作记录已全部清理。

## 设计证据

见 `frontend/design-qa.md`，最终结果为 `passed`。

## 已知边界

- 当前规则只支持四类结构化条件和 AND，不支持表达式、脚本、正则、OR 或内置模板；
- 当前没有周期性执行发布规则的候选生成器；
- 当前没有 Incident 列表、去重、确认、误报、合并或关闭流程。
