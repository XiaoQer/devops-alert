# 告警中心与告警源管理验收记录

## 验收范围

本次验收覆盖告警源身份与凭据、动态接入、Receipt、告警读取 API、告警中心、接入源管理和现有事故中心回归。验收使用本地 MySQL 8.4、真实 FastAPI 服务和 Vue 开发服务器，不使用演示数据。

## 自动化验证

- 后端统一脚本：Ruff、123 个文件格式检查、75 个源文件 Mypy、MySQL 迁移与全部测试通过；403 项测试通过，覆盖率 92.31%。
- 前端统一验证：14 个测试文件、52 项 Vitest 全部通过；Vite 生产构建成功。
- Sites Worker：4 项测试通过，覆盖静态资源、应用路由回退、API 不回退应用壳和打包文件完整性。
- 构建安全扫描：`II_FRONTEND_API_TOKEN`、内部认证主体、实验身份字段和具体 Token 样本均为零命中。
- 当前迁移版本：`0005_alert_sources`。

## 真实 MySQL 接入链路

专项测试使用独立随机测试库和进程内秘密，未输出或保存 Token：

- 两个同类型来源发送相同外部事件身份，得到不同 Alert ID；
- 精确重放前后 SignalEvent 数量不变，并新增安全 REPLAYED Receipt；
- 每次已认证请求产生一条有界 Receipt；
- 满足门槛的真实 Alertmanager 告警完成事故关联且没有创建 DiagnosisRun；
- 验证入口前后 SignalEvent、Alert、关联任务和 Incident 数量不变。

专项命令共 4 项测试，全部通过。

## 真实浏览器旅程

验收地址为 `http://127.0.0.1:5173/`，后端为 `http://127.0.0.1:8000/`。开发代理仅在请求侧注入进程内临时人工 Token。

- 事故中心读取真实 MySQL 事故、处置阶段、活动、关联解释和关联告警；
- 告警中心展示 24 小时概况、真实告警列表、实际检测结果、来源、中文系统处理过程和关联事故；
- 严重程度筛选只保留符合条件的告警；从告警详情成功进入对应事故；
- 接入源页面展示三个系统兼容来源和用户来源，并明确区分系统管理与用户管理；
- 创建用户来源后只出现一次 Token 确认页，确认后页面中不再存在 Token；
- 轮换后出现新的单次确认页，旧凭据继续有效；撤销旧凭据后只剩一个有效凭据；
- 尝试撤销最后一个有效凭据返回固定中文保护说明，来源和凭据事实保持不变；
- 停用与重新启用均由后端保存并重新读取最终事实；
- 安全失败 Receipt 使来源显示“最近接收失败”，页面展示“请求被拒绝”和固定计数，不展示请求正文；
- 后端和前端停止后，页面显示安全的读取失败状态与重新加载入口，不出现演示数据；修正后读取失败不再使用“写操作结果未知”文案；
- 服务恢复后页面重新读取真实事故；浏览器控制台错误和警告为零。

本次浏览器验收创建的非敏感测试来源为 `src_e1d9404c53034f6388a57f5adf4cbd42`。验收记录不包含 Token、摘要、请求正文、请求头或外部 URL。

## 结论与已知边界

活跃规格中的来源隔离、一次性秘密、动态接入、幂等重放、安全 Receipt、告警读取和两类前端页面验收条件均有自动化或真实操作证据。告警人工状态、自动取证、AI、生产身份和部署仍不在本阶段范围内。
